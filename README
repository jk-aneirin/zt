csvhandler.py：对文件LOG_DETAIL_20151209112109.csv进行分析，提取里面有效的域名邮箱，然后将其插入redis数据库中
findPath.py：使用BeautifulSoap对jenkins服务器的WEB界面进行提取，将指定SVNID的提交代码提取出来，然后对格式进行修改，使其满足tomcat服务器的目录要求(脚本执行的参数为提交代码的SVNID)
postfix_status.sh:该脚本用在zabbix的postfix监控中，如果脚本执行返回1则说明postfix进程停止了，如果返回0则说明程序正常运行
filecp.sh:向目标服务器上传文件，服务器间需做RSA认证
